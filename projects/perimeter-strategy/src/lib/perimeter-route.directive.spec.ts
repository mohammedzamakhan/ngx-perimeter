import { PerimeterRouteDirective } from './perimeter-route.directive';

describe('PerimeterRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new PerimeterRouteDirective();
    expect(directive).toBeTruthy();
  });
});
